[pytest]
testpaths = tests
python_files = test_*.py _test_*.py
python_functions = test_*
asyncio_mode = auto
addopts = --verbose
markers =
    unit: isolated unit tests (default)
    integration: marks integration tests across API/Dagster boundaries
    validation: marks DB-backed data quality/regression checks

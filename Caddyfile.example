# =============================================================================
# Caddy Configuration for Process Metrics Platform
# =============================================================================
# Required env vars (from .env.prod):
#   MB_DOMAIN
#   DAGSTER_DOMAIN
#   API_DOMAIN
#   ADMIN_DOMAIN
#   DAGSTER_BASIC_AUTH_HASH
#   SWAGGER_BASIC_AUTH_HASH
# =============================================================================

{$MB_DOMAIN} {
    reverse_proxy metabase:3000
    header {
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
        Referrer-Policy "strict-origin-when-cross-origin"
    }
}

{$DAGSTER_DOMAIN} {
    basicauth * {
        admin {$DAGSTER_BASIC_AUTH_HASH}
    }
    reverse_proxy dagster-webserver:3000
    header {
        X-Content-Type-Options "nosniff"
    }
}

{$API_DOMAIN} {
    @swagger path /docs /redoc /openapi.json
    basicauth @swagger {
        admin {$SWAGGER_BASIC_AUTH_HASH}
    }
    reverse_proxy app:8000
    header {
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
    }
}

{$ADMIN_DOMAIN} {
    reverse_proxy admin-ui:8501
    header {
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
    }
}

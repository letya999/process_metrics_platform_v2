[project]
name = "orchestrator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.2,<3.0.0",
    "python-dotenv>=1.0.0,<2.0.0",
]

[build-system]
requires = ["pdm-pep517"]
build-backend = "pdm.pep517.api"

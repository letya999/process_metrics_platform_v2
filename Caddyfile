# =============================================================================
# Caddy Configuration for Process Metrics Platform
# Using nip.io for secure HTTPS access via IP: 134.209.90.127
# =============================================================================

# Metabase (BI Dashboard)
http://metrics.134.209.90.127.nip.io {
    reverse_proxy metabase:3000

    header {
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
        Referrer-Policy "strict-origin-when-cross-origin"
    }
}

# Dagster UI (ETL monitoring)
http://dagster.134.209.90.127.nip.io {
    # Basic Auth for security
    # Replace the hash below with your own (generated via 'caddy hash-password')
    basicauth * {
        admin $2a$14$PhYoC4cISk0DMibmnE7ozuiPjO18h2u26BLdnJ5FbSVzh5urqYsi6
    }

    reverse_proxy dagster:3000

    header {
        X-Content-Type-Options "nosniff"
    }
}

# FastAPI (Admin Panel / API)
http://api.134.209.90.127.nip.io {
    reverse_proxy app:8000

    header {
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
    }
}
